# {{ task_name }}

Status: DEVNET

{{ adapter_description }}

> This task targets a devnet. The `DEVNET` status keeps it out of stacked
> simulations of the parent network (see `fetch-tasks.sh`); `just simulate`,
> `just sign`, `just approve`, and `just execute` continue to work directly
> from this directory.

## Devnet Context

| Field              | Value                                  |
| ------------------ | -------------------------------------- |
| Devnet             | `{{ devnet.name }}` ({{ devnet.type }}) |
| Scope              | {{ devnet.scope_url or "—" }}          |
| L1                 | {{ devnet.l1.name }}                   |
| Chains             | {% for c in devnet.chains %}{{ c.name }} ({{ c.chain_id }}){% if not loop.last %}, {% endif %}{% endfor %} |
{% if cannon_prestate %}| Prestate (cannon64) | `{{ cannon_prestate }}` ({{ "overridden" if cannon_prestate_overridden else "from devnet" }}) |
{% endif %}{% if cannon_kona_prestate %}| Prestate (cannonKona) | `{{ cannon_kona_prestate }}` ({{ "overridden" if cannon_kona_prestate_overridden else "from devnet" }}) |
{% endif %}{% if opcm %}| Target OPCM        | `{{ opcm }}` ({{ "overridden" if opcm_overridden else "from devnet" }}) |
{% endif %}

_Generated by `just gen-devnet-task` on {{ generated_on }}. Re-run with the same args to regenerate._

## Simulation

Devnet tasks are not part of `simulate-stack sep`. Run this task directly from
its directory:

```bash
cd {{ task_rel_path }}
just --dotenv-path $(pwd)/.env simulate
```

## Validation

See `VALIDATION.md` for expected state changes and validation steps.
