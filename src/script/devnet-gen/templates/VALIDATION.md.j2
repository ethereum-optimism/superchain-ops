# Validation: {{ task_name }}

This document describes the expected state changes for this devnet task. Fill
out the sections below before signing or executing.

## Expected Domain and Message Hashes

> Note: devnet tasks may skip the formal hash validation that mainnet tasks
> require. Document the hashes you observe during simulation here so reviewers
> can confirm them.

- Domain hash: `0x...`
- Message hash: `0x...`

## Expected State Changes

For each contract touched by the upgrade, list the slot-level changes you
expect to see (or paste the diff from a clean simulation run).

### Per-chain

{% for chain in devnet.chains %}
#### {{ chain.name }} ({{ chain.chain_id }})

- _TBD_

{% endfor %}

## Expected Validation Errors

Once a clean simulation has been run, copy the validator's expected error
string into `[[opcmUpgrades]].expectedValidationErrors` in `config.toml` for
each chain so future runs assert against it.
