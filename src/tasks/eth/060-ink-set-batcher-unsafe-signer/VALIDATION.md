# Validation

This document can be used to validate the inputs and result of the execution of the transaction which you are signing.

The steps are:

1. [Validate the Domain and Message Hashes](#expected-domain-and-message-hashes)
2. [Transaction Inputs](config.toml): inputs can be verified in the config.toml file.
3. State Changes: the template's `_validate` block asserts `SystemConfig.batcherHash()` and `SystemConfig.unsafeBlockSigner()` equal the configured values. State changes can also be reviewed in Tenderly via the link printed during simulation.

## Expected Domain and Message Hashes

First, validate the domain and message hashes. These values should match both the values on your ledger and the values printed to the terminal when you run the task.

> [!CAUTION]
>
> Before signing, ensure the below hashes match what is on your ledger.
>
> ### OPE Safe (`TODO(§3.3)`)
>
> - Domain Hash:  `TODO — regenerate with just simulate once the OPE Safe address and nonce are known`
> - Message Hash: `TODO`
> - Safe Hash:    `TODO`
>
> _The Domain Hash is deterministic for the OPE Safe on Ethereum mainnet (chainId 1): `keccak256(abi.encode(0x47e7…9218, 1, <OPE Safe>))`. The Message and Safe hashes must be generated by a successful `just simulate` run with the [config.toml](./config.toml) state overrides (OPE Safe nonce; `SystemConfig.owner` → OPE Safe). They are valid only for those overrides — re-run `just simulate` and replace them if the OPE Safe nonce has advanced or the owner override is removed (after W26) before signing._

## Understanding Task Calldata

The task batches two `SystemConfig` setters through Multicall3, targeting the Ink mainnet `SystemConfigProxy` (`0x62C0a111929fA32ceC2F76aDba54C16aFb6E8364`).

Verify the two inner calldata fingerprints (fill the OPE addresses first):

```bash
# setBatcherHash(bytes32) — OPE batcher address left-padded to 32 bytes
cast calldata "setBatcherHash(bytes32)" 0x000000000000000000000000<OPE_BATCHER>
# selector: 0xc9b26f61

# setUnsafeBlockSigner(address) — OPE sequencer
cast calldata "setUnsafeBlockSigner(address)" <OPE_SEQUENCER>
# selector: 0x18d13918
```

The outer Multicall3 `aggregate3` blob is assembled by the framework and printed during simulation; confirm it contains both inner calls above and no others.

## Task State Changes

### `0x62C0a111929fA32ceC2F76aDba54C16aFb6E8364` (SystemConfigProxy) — Chain ID 57073

- `batcherHash()` updates from the current Gelato batcher (CONFIRM on-chain) to the OPE batcher.
- `unsafeBlockSigner()` updates from the current Gelato unsafe block signer (CONFIRM on-chain) to the OPE sequencer.

### OPE Safe (`TODO(§3.3)`)

Nonce increments by 1.

> [!NOTE]
> The `SystemConfig.owner()` change Gelato → OPE Safe (slot `0x33`) is **not** part of this task; it is performed outside the ops repo by Gelato (plan step W26). The owner override in [config.toml](./config.toml) exists only so the setters authorize during simulation, and must be removed once W26 is executed on-chain.

## Post-execution verification

```bash
cast call 0x62C0a111929fA32ceC2F76aDba54C16aFb6E8364 "batcherHash()(bytes32)" --rpc-url <MAINNET_RPC>
# Expected: 0x000000000000000000000000<OPE batcher>
cast call 0x62C0a111929fA32ceC2F76aDba54C16aFb6E8364 "unsafeBlockSigner()(address)" --rpc-url <MAINNET_RPC>
# Expected: <OPE sequencer>
```
