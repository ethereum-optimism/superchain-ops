# 072-arena-z-fee-vault-update

Status: DRAFT, NOT READY TO SIGN

## Objective

Updates the fee vault recipient address for Arena-Z Testnet (Chain ID: 9899) on Sepolia.

This is part of the Arena-Z Chain Servicer Migration. The fee recipient for all three fee vaults
(SequencerFeeVault, BaseFeeVault, L1FeeVault) is updated from `0x9648CF2B93DdBbe120812a567294B2954bCc8568`
to `0xE75f598754A552841E65f43197C85028874A96a4`. The withdrawal network is also changed from L2 (`1`) to L1 (`0`).

Since the fee vaults use immutable recipients (baked into constructor bytecode), new implementation
contracts were pre-deployed on L2 before this task. This task only upgrades the 3 vault proxies
to point to the pre-deployed implementations via L1→L2 deposit transactions through the OptimismPortal.

### Required Steps

For each of the three fee vaults:
1. Upgrade the vault proxy via L2 ProxyAdmin (through OptimismPortal deposit)

## Transaction creation

The transaction is generated by the [UpdateFeeVaultRecipient template](../../../template/UpdateFeeVaultRecipient.sol),
which reads the inputs from the [`config.toml`](./config.toml) file.

## Signing and execution

Follow the instructions in the [Nested Execution](../../../NESTED.md) guide.

## Simulation

```
cd src/tasks/sep/072-arena-z-fee-vault-update

# Foundation
just --dotenv-path $(pwd)/.env --justfile ../../../justfile sign foundation

# Security Council
just --dotenv-path $(pwd)/.env --justfile ../../../justfile sign council

# Simulate without ledger
SIMULATE_WITHOUT_LEDGER=1 just --dotenv-path $(pwd)/.env --justfile ../../../justfile simulate foundation
```
