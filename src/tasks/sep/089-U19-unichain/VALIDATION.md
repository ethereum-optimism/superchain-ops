# Validation

This document can be used to validate the inputs and result of the execution of the upgrade transaction which you are signing.

> [!CAUTION]
> These hashes were generated with **placeholder prestates** (`0xdead...`). They will change when real prestates are filled in.
>
> Additionally, task 089 (Unichain) cannot be simulated standalone — it requires tasks 086–088 to have run first (upgrading the shared SuperchainConfig). Re-run `just simulate-stack sep 089-U19-unichain` once those tasks are executed on-chain or after the negative-gas-refund stacking issue is resolved.

## Expected Domain and Message Hashes

TODO: populate after full stacked simulation (086 → 087 → 088 → 089) succeeds.

## Task Calldata

TODO: fill in calldata after simulation with real prestates.
