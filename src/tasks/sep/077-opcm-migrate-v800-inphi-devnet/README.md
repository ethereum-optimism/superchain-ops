# 077-opcm-migrate-v800-inphi-devnet

Status: DEVNET

Migrate V800/Super Root OP chains into a shared dispute game set (op-contracts/v7.1.x).

> This task targets a devnet. The `DEVNET` status keeps it out of stacked
> simulations of the parent network (see `fetch-tasks.sh`); `just simulate`,
> `just sign`, `just approve`, and `just execute` continue to work directly
> from this directory.

## Devnet Context

| Field              | Value                                  |
| ------------------ | -------------------------------------- |
| Devnet             | `inphi-devnet` (alphanet) |
| Scope              | —          |
| L1                 | sepolia                   |
| Chains             | inphi-devnet-0 (420120092), inphi-devnet-1 (420120093) |
| Prestate (cannonKona) | `0x03b483ec22e6620627670d3d9902d2088977310999a9d1ff70711ed504fa49a3` (from devnet) |
| Target OPCM        | `0x0d38af819dd2a486eba94bb5a723d95b2ef326dc` (overridden) |


_Generated by `just gen-devnet-task` on 2026-05-11. Re-run with the same args to regenerate._

## Simulation

Devnet tasks are not part of `simulate-stack sep`. Run this task directly from
its directory:

```bash
cd src/tasks/sep/077-opcm-migrate-v800-inphi-devnet
just --dotenv-path $(pwd)/.env simulate
```

## Validation

See `VALIDATION.md` for expected state changes and validation steps.
