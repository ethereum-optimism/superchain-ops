# l2Chains is only here so we can reuse this fixture for both SimpleAddressRegistry and SuperchainAddressRegistry tests.
l2chains = [{ name = "Base", chainId = 8453 }]

[addresses]
Alice = "not_a_valid_address"