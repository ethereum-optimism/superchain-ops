# this is the file used to determine the network configuration

l2chains = [{name = "Test Sepolia Devnet", chainId = 1135}]

templateName = "SafeOwnerThresholdTemplate"

# example values for safeAddressString = "NESTED_SAFE_1", "NESTED_SAFE_2", "NESTED_SAFE_3",.. "PARENT_SAFE"
safeConfig = {safeAddressString = "NESTED_SAFE_1", addressesToAdd = [], addressesToRemove = [], newThreshold = 1}
