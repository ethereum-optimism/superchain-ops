version: 2.1

orbs:
  go: circleci/go@1.8.0

jobs:
  sc-rehearsal-test:
    working_directory: ~/superchain-ops
    docker:
      - image: cimg/rust:1.73
    steps:
      - checkout:
          path: ~/superchain-ops
      - run: cargo install just
      - run: just install-foundry

workflows:
  main:
    jobs:
      - sc-rehearsal-test
